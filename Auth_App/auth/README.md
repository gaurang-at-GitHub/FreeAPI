# Authentication App

A simple React app that handles user authentication.

## Features
- Login and logout functionality
- Token-based auth stored in local storage
- Fetches data from FreeAPI

## Run Locally
1. `npm install`
2. `npm run dev`
